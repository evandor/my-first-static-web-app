{% comment %}
- Description: This liquid file just formats a given number with a two digit fraction part and a plus sign. The display of the unit is optional.
- Parameters
  - number value that should be formatted
  - displayUnit, flag indicating if the unit should be displayed 
{% endcomment %}
{%- unless number -%}
    {{ "---" }}
{%- else -%}
    {%- if displayUnit -%}{{- number | format_number: 'N2' | append: " D" | prepend: "+" | default: "---" -}}
    {%- else -%}{{- number | format_number: 'N2' | prepend: "+" | default: "---" -}}{%- endif -%}
{%- endunless -%}    
